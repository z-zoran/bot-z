
export function handlePlay(/*kolekcija, start, koliko*/) {

    // UBACITI LOGIKU

    /*
    let data = {
        zahtjev: 'dajKendlove',
        timestamp: Date.now(),
        kolekcija: kolekcija,
        start: start,
        koliko: koliko,
    };
    return await fetch('/', {
        method: 'POST', // *GET, POST, PUT, DELETE, etc.
        body: JSON.stringify(data), // must match 'Content-Type' header
        headers: {
            'User-agent': 'Mozilla/4.0',
            'Content-type': 'application/json',
        },
    }).then(response => response.json())
        .then(JSON.parse)
    */
}